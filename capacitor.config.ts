import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.estadisticasvoley.app',
  appName: 'Estadisticas Voley',
  webDir: 'build'
};

export default config;
